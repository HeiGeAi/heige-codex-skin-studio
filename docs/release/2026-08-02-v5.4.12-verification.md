# v5.4.12 发布审查

日期：2026-08-02

## 版本与制品

- 版本：`5.4.12`
- 基线：`v5.4.11`
- 制品：`output/heige-codex-skin-studio.skill`
- 制品 SHA-256：`cabf3d75cc05201c91157f545f9a4708da2f907c3e02ec5d151da59693ca4abd`

## 代码审查

- 侧栏、输入框、用户气泡和审批表面关闭 `backdrop-filter`，避免滚动和流式输出持续采样动态背景。
- 主内容背景补回新版 Codex 的 `[data-app-shell-main-surface="default"]` 选择器。
- `skinStatus` 不再调用 `Page.bringToFront`，健康巡检不会抢前台焦点。
- 非持久会话的健康巡检走轻量无租约路径。
- 回归测试覆盖 CSS 模糊禁用、状态读取不抢焦点和 retained session 巡检。

## 自动化门禁

- `npm test`：1041 项，1034 通过，0 失败，7 跳过。
- `npm run release:check`：通过。
- 公开素材权利门禁：39 个视觉素材全部接受。
- `git diff --check`：通过。

## 本机活体验收

- Codex：`26.727.51351`
- CDP：`127.0.0.1:9341`，进程归属匹配。
- v5.4.12 源码实际注入：主题 `dalao-dianyan`，菜单存在，状态 `active`。
- 实际计算样式：侧栏和输入框 `backdrop-filter` 均为 `none`。
- 同一滚动动作：`4718.7105 ms`。

## 未完成边界

完整 `live-macos-acceptance` harness 的预检未运行成功，因为当前机器已经没有旧的
`com.heige.codex-skin-watchdog.plist`，预检在读取旧迁移现场时返回 `ENOENT`。这属于
已迁移机器与旧 harness 前置条件不一致，不是本次 v5.4.12 代码测试失败。

本轮没有发送真实聊天消息，也没有宣称真实流式回复已经完成端到端验收。
